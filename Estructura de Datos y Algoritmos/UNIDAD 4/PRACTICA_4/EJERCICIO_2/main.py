"""Ejercicio Nº 2: Codifique un programa que utilice el algoritmo de Huffman  para comprimir un archivo de 
caracteres ya generado. Nota: hallar la frecuencia de cada carácter"""

from arbolBB import ArbolBinarioBusqueda
from listaEncadenada import listaEncadenada

"""Paso 1: Generar una lista de caracteres, ordenada en forma creciente por sus
frecuencias de ocurrencia."""

"""En esta lista, cada celda es un árbol binario, formado por un solo nodo, raíz del árbol."""

class Huffman:
   __texto = ""
   __frecuencias = []
   __arbol = ArbolBinarioBusqueda()
   __tabla = []

   def __init__(self, texto):
      self.__texto = texto
      self.__frecuencias 
      self.__arbol = self
      self.__tabla = self
   
   def 
   
if __name__ == "__main__":
   texto = "Existen distintos tipos de poemas según su género o según el tipo de composición"
   
